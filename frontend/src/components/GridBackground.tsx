/**
 * Full-viewport subtle grid overlay.
 * Masked with a radial gradient so it fades out toward the edges.
 */
export function GridBackground() {
  return <div className="grid-bg" aria-hidden="true" />
}
