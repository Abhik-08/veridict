# Knowledge

This directory contains benchmark datasets and loaders used by Veridict for AI response evaluation.

## Structure

- **datasets/** — Raw benchmark datasets
  - `truthfulqa/` — TruthfulQA dataset
  - `squad/` — SQuAD dataset
- **loaders/** — Scripts to load and preprocess datasets
  - `dataset_loader.py` — Dataset loading utilities
