/**
 * Full-viewport animated radial gradient background.
 * Renders a fixed layer with softly moving gradient orbs.
 */
export function AnimatedBackground() {
  return <div className="animated-bg" aria-hidden="true" />
}
